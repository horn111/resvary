# @resvary/postgres

Postgres `CreditStore` and `ReceiptStore` implementations for Resvary design-partner deployments.

Run `resvary-postgres migrate` before starting an application. Stores never migrate automatically.
